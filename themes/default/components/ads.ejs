<% if (settings.ads.enabled == true) { %>
         <div class="row ">
              <div class="col-12 grid-margin">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Ads</h4>
                    <div class="table-responsive">
                      <%= settings.ads.script %>
                    <!-- Votre code de pub (google, adsenses, ect) -->
                    </div>
                  </div>
                </div>
              </div>
            </div>
<% } %>
